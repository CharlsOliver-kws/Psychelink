# 测试环境：跳过知识库导入（避免依赖 Milvus / 外部 Embedding 服务）
app.kb.auto-import=false
# 哑 API key：避免 Spring AI 在上下文加载阶段因缺 key 失败（测试不发起真实 LLM 调用）
spring.ai.openai.api-key=test-key
app.admin.username=test-admin
app.admin.password=test-admin-pass
# 固定 JWT 密钥（仅测试）
app.jwt.secret=psychelink-test-secret-0123456789-0123456789
